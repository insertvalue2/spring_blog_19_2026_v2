# 구매 내역 조회 기능 작업 내용

## 📋 개요

사용자가 구매한 유료 게시글의 내역을 조회하는 기능입니다. 
로그인한 사용자만 자신의 구매 내역을 확인할 수 있습니다.

---

## 🎯 주요 기능

- 로그인한 사용자의 구매 내역 목록 조회
- 구매한 게시글 정보 표시 (제목, 작성자, 구매일시, 구매 가격)
- 구매 내역이 없을 경우 안내 메시지 표시

---

## 📁 관련 파일 구조

```
src/main/java/org/example/demo_ssr_v1_1/
├── user/
│   └── UserController.java          # 구매 내역 조회 엔드포인트
├── purchase/
│   ├── Purchase.java                # 구매 내역 엔티티
│   ├── PurchaseRepository.java      # 구매 내역 데이터 조회
│   ├── PurchaseService.java         # 구매 내역 비즈니스 로직
│   └── PurchaseResponse.java        # 구매 내역 응답 DTO
└── _core/
    └── utils/
        └── MyDateUtil.java          # 날짜 포맷팅 유틸리티

src/main/resources/templates/
└── user/
    └── purchase-list.mustache        # 구매 내역 화면 템플릿
```

---

## 🔄 동작 흐름

```
1. 사용자 요청
   GET /user/purchase/list
   ↓
2. UserController.purchaseList()
   - 세션에서 로그인 사용자 정보 추출
   - PurchaseService.구매내역조회() 호출
   ↓
3. PurchaseService.구매내역조회()
   - PurchaseRepository.findAllByUserIdWithBoard() 호출
   - Purchase 엔티티 리스트를 DTO로 변환
   ↓
4. PurchaseRepository.findAllByUserIdWithBoard()
   - JOIN FETCH로 Purchase, Board, User 한 번에 조회
   - 생성일 기준 내림차순 정렬 (최신순)
   ↓
5. PurchaseResponse.ListDTO 변환
   - Purchase 엔티티 → ListDTO 변환
   - 날짜 포맷팅 처리
   ↓
6. View 렌더링
   - purchase-list.mustache 템플릿에 데이터 전달
   - 구매 내역 목록 화면 표시
```

---

## 💻 코드 상세 설명

### 1. Controller Layer

**파일**: `UserController.java`

```java
@GetMapping("/user/purchase/list")
public String purchaseList(Model model, HttpSession session) {
    User sessionUser = (User) session.getAttribute("sessionUser");
    List<PurchaseResponse.ListDTO> purchaseList = purchaseService.구매내역조회(sessionUser.getId());
    model.addAttribute("purchaseList", purchaseList);
    return "user/purchase-list";
}
```

**역할**:
- HTTP GET 요청 처리
- 세션에서 로그인 사용자 정보 추출
- Service 레이어에 비즈니스 로직 위임
- View에 데이터 전달

**인증/인가**:
- `LoginInterceptor`가 `/user/**` 경로를 인터셉트하여 로그인 여부 확인
- 로그인하지 않은 사용자는 `/login` 페이지로 리다이렉트

---

### 2. Service Layer

**파일**: `PurchaseService.java`

```java
@Transactional(readOnly = true)
public List<PurchaseResponse.ListDTO> 구매내역조회(Long userId) {
    List<Purchase> purchaseList = purchaseRepository.findAllByUserIdWithBoard(userId);

    return purchaseList.stream()
            .map(PurchaseResponse.ListDTO::new)
            .toList();
}
```

**역할**:
- 비즈니스 로직 처리
- Repository에서 구매 내역 조회
- 엔티티를 DTO로 변환하여 반환

**트랜잭션**:
- `@Transactional(readOnly = true)`: 읽기 전용 트랜잭션
- 조회만 수행하므로 읽기 전용으로 설정하여 성능 최적화

**OSIV False 환경 대응**:
- 트랜잭션 내에서 필요한 데이터를 모두 조회
- 엔티티를 DTO로 변환하여 반환 (LAZY 로딩 문제 방지)

---

### 3. Repository Layer

**파일**: `PurchaseRepository.java`

```java
@Query("""
    SELECT p FROM Purchase p
    JOIN FETCH p.board b 
    JOIN FETCH p.user u 
    WHERE p.user.id = :userId 
    ORDER BY p.createdAt DESC """)
List<Purchase> findAllByUserIdWithBoard(@Param("userId") Long userId);
```

**역할**:
- 사용자 ID로 구매 내역 조회
- JOIN FETCH를 사용하여 연관된 엔티티를 한 번에 조회

**JOIN FETCH 사용 이유**:
- **N+1 문제 해결**: Purchase 조회 후 Board, User를 개별 쿼리로 조회하는 것을 방지
- **OSIV False 환경 대응**: 트랜잭션 종료 전에 필요한 데이터를 모두 로딩
- **성능 최적화**: 한 번의 쿼리로 모든 필요한 데이터 조회

**생성되는 SQL**:
```sql
SELECT p.*, b.*, u.* 
FROM purchase_tb p
INNER JOIN board_tb b ON p.board_id = b.id
INNER JOIN user_tb u ON p.user_id = u.id
WHERE p.user_id = ?
ORDER BY p.created_at DESC
```

---

### 4. Response DTO

**파일**: `PurchaseResponse.java`

```java
@Data
public static class ListDTO {
    private Long id;              // 구매내역 ID
    private Long boardId;          // 게시글 ID
    private String boardTitle;     // 게시글 제목
    private String boardAuthor;    // 게시글 작성자명
    private Integer price;         // 구매 가격
    private String purchasedAt;   // 구매일시 (포맷된 날짜)

    public ListDTO(Purchase purchase) {
        this.id = purchase.getId();
        this.price = purchase.getPrice();
        
        // 구매 일시 포맷팅
        if (purchase.getCreatedAt() != null) {
            this.purchasedAt = MyDateUtil.timestampFormat(purchase.getCreatedAt());
        }

        // 게시글 정보 (JOIN FETCH로 이미 로딩된 board 사용)
        if (purchase.getBoard() != null) {
            this.boardId = purchase.getBoard().getId();
            this.boardTitle = purchase.getBoard().getTitle();

            // 게시글 작성자 정보 (JOIN FETCH로 이미 로딩된 user 사용)
            if (purchase.getBoard().getUser() != null) {
                this.boardAuthor = purchase.getBoard().getUser().getUsername();
            }
        }
    }
}
```

**역할**:
- Purchase 엔티티를 화면에 표시할 수 있는 DTO로 변환
- 날짜 포맷팅 처리
- 필요한 정보만 추출하여 전달

**날짜 포맷팅**:
- `MyDateUtil.timestampFormat()`: `yyyy-MM-dd HH:mm` 형식으로 변환
- 예: `2024-01-15 14:30`

---

### 5. Entity

**파일**: `Purchase.java`

```java
@Entity
@Table(
    name = "purchase_tb",
    uniqueConstraints = {
        @UniqueConstraint(name = "uk_user_board", columnNames = {"user_id", "board_id"})
    }
)
public class Purchase {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id")
    private User user;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "board_id")
    private Board board;

    private Integer price;  // 구매 시 지불한 포인트

    @CreationTimestamp
    private Timestamp createdAt;
}
```

**역할**:
- 구매 내역 정보를 저장하는 엔티티
- User와 Board의 Many-to-Many 관계를 중간 테이블로 구현

**유니크 제약조건**:
- `(user_id, board_id)`: 한 사용자가 같은 게시글을 중복 구매할 수 없도록 제한

**연관관계**:
- `Purchase -> User` (ManyToOne): 구매한 사용자
- `Purchase -> Board` (ManyToOne): 구매한 게시글
- LAZY 로딩 사용 (필요할 때만 조회)

---

### 6. View Template

**파일**: `purchase-list.mustache`

```mustache
{{#purchaseList}}
<div class="card mb-4">
    <div class="card-body">
        <h5 class="card-title">
            <a href="/board/{{boardId}}">{{boardTitle}}</a>
        </h5>
        <p class="text-muted">작성자: {{boardAuthor}}</p>
        <p class="text-muted">구매일: {{purchasedAt}}</p>
        <span class="badge bg-primary">-{{price}} P</span>
    </div>
</div>
{{/purchaseList}}

{{^purchaseList}}
<div class="text-center py-5">
    <h5 class="text-muted">구매 내역이 없습니다</h5>
    <a href="/board/list" class="btn btn-primary">게시글 목록으로</a>
</div>
{{/purchaseList}}
```

**역할**:
- 구매 내역 목록을 화면에 표시
- 구매 내역이 없을 경우 안내 메시지 표시
- 각 구매 내역을 카드 형태로 표시

---

## 🔑 핵심 기술 포인트

### 1. JOIN FETCH 사용

**문제점 (N+1 문제)**:
```java
// 나쁜 예: N+1 문제 발생
List<Purchase> purchases = purchaseRepository.findByUserId(userId);
// Purchase 조회: 1번 쿼리
// 각 Purchase마다 Board 조회: N번 쿼리
// 각 Board마다 User 조회: N번 쿼리
// 총 1 + N + N = 2N + 1번 쿼리 실행
```

**해결책 (JOIN FETCH)**:
```java
// 좋은 예: 한 번의 쿼리로 모든 데이터 조회
@Query("""
    SELECT p FROM Purchase p
    JOIN FETCH p.board b 
    JOIN FETCH p.user u 
    WHERE p.user.id = :userId """)
List<Purchase> findAllByUserIdWithBoard(@Param("userId") Long userId);
// 총 1번의 쿼리로 모든 데이터 조회
```

### 2. OSIV False 환경 대응

**OSIV (Open Session In View) False 설정)**:
```yaml
spring:
  jpa:
    open-in-view: false
```

**영향**:
- 트랜잭션이 종료되면 영속성 컨텍스트가 닫힘
- LAZY 로딩이 트랜잭션 밖에서 실행되면 `LazyInitializationException` 발생

**대응 방법**:
1. JOIN FETCH로 필요한 데이터를 트랜잭션 내에서 모두 조회
2. 엔티티를 DTO로 변환하여 반환 (LAZY 로딩 불필요)

### 3. Stream을 활용한 DTO 변환

```java
return purchaseList.stream()
        .map(PurchaseResponse.ListDTO::new)  // 메서드 참조 사용
        .toList();
```

**장점**:
- 간결하고 읽기 쉬운 코드
- 함수형 프로그래밍 스타일
- 불변 컬렉션 생성 (`toList()`는 Java 16+)

---

## 📊 데이터베이스 스키마

### purchase_tb 테이블

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| id | BIGINT | 기본키 (자동 증가) |
| user_id | BIGINT | 구매한 사용자 ID (FK) |
| board_id | BIGINT | 구매한 게시글 ID (FK) |
| price | INT | 구매 시 지불한 포인트 |
| created_at | TIMESTAMP | 구매 일시 (자동 생성) |

**유니크 제약조건**:
- `(user_id, board_id)`: 한 사용자가 같은 게시글을 중복 구매할 수 없음

---

## ✅ 테스트 시나리오

### 정상 케이스

1. **로그인한 사용자가 구매 내역 조회**
   - 요청: `GET /user/purchase/list`
   - 결과: 구매한 게시글 목록이 최신순으로 표시됨

2. **구매 내역이 있는 경우**
   - 게시글 제목, 작성자, 구매일시, 구매 가격이 표시됨
   - 게시글 제목 클릭 시 해당 게시글 상세 페이지로 이동

3. **구매 내역이 없는 경우**
   - "구매 내역이 없습니다" 메시지 표시
   - "게시글 목록으로" 버튼 제공

### 예외 케이스

1. **로그인하지 않은 사용자**
   - `LoginInterceptor`가 요청을 차단
   - `/login` 페이지로 리다이렉트

2. **세션 만료**
   - `Exception401` 발생
   - 로그인 페이지로 리다이렉트

---

## 🎓 학습 포인트

### 1. 계층 구조 (3-Tier Architecture)

```
Controller (표현 계층)
    ↓
Service (비즈니스 계층)
    ↓
Repository (데이터 접근 계층)
    ↓
Database
```

각 계층의 역할을 명확히 분리하여 유지보수성 향상

### 2. DTO 패턴

- 엔티티를 직접 반환하지 않고 DTO로 변환
- 필요한 정보만 노출하여 보안성 향상
- 화면에 맞는 데이터 구조 제공

### 3. JOIN FETCH 최적화

- N+1 문제 해결
- 성능 최적화
- OSIV False 환경에서 안전한 데이터 조회

### 4. 트랜잭션 관리

- 읽기 전용 트랜잭션으로 성능 최적화
- 트랜잭션 범위를 적절히 설정하여 데이터 일관성 보장

---

## 📝 참고 사항

- 구매 내역은 생성일 기준 내림차순으로 정렬 (최신순)
- 구매 가격은 음수로 표시 (`-500 P`)
- 게시글 제목 클릭 시 해당 게시글 상세 페이지로 이동
- 구매 내역이 없을 경우 안내 메시지와 함께 게시글 목록으로 이동할 수 있는 버튼 제공

---

## 🔗 관련 기능

- **구매하기**: `PurchaseService.구매하기()` - 유료 게시글 구매
- **구매 여부 확인**: `PurchaseService.구매여부확인()` - 게시글 상세 페이지에서 구매 여부 확인

---

**작성일**: 2024년  
**작성자**: 개발팀  
**버전**: 1.0

