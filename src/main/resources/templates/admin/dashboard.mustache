{{> layout/header}}

<div class="container p-5">
    <div class="card">
        <div class="card-header">
            <h2 class="mb-0">관리자 대시보드</h2>
        </div>
        <div class="card-body">
            <p class="mb-1">안녕하세요, <b>{{adminName}}</b> 님.</p>
            <p class="text-muted mb-0">이 페이지는 관리자만 접근 가능합니다.</p>
        </div>
    </div>
</div>

{{> layout/footer}}


